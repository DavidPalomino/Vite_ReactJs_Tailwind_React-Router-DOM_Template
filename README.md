# ReactJs + Vite + Tailwind + React-Router-DOM

Install dependencies 
```
npm i 
```

Run proyect
```
npm run dev 
```

Change the remote origin
```
git remote delete origin
```
Add your repository as origin remote
```
git remote add origin YOUR_REPOSITORY
```

By: David Palomino 
